import { ISelectDialogItem } from '../scroll_select_dialog/ScrollSelectDialog'

export class SelectDialogListCreator {
    /* example
    public static provincesList(map: Map<number, Province>) {
        const arr = []
        for (const [key] of map) {
            arr.push({
                label: map.get(key).getName(),
                key: String(map.get(key).getObjectId())
            })
        }
        return arr
    }
    */
}
